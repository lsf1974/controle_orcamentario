# ── Frontend (public vars only — safe to give to the frontend container) ──────
NEXT_PUBLIC_API_URL="http://localhost:3001"
NEXT_PUBLIC_WS_URL="ws://localhost:3001"

# ── Ambiente ──────────────────────────────────────────────────────────────────
NODE_ENV="production"
PORT=3000
FRONTEND_URL="http://localhost:3000"
